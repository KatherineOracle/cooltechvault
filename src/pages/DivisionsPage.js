import DivisionList from '../components/Divisions/DivisionList';
/**
 * Container Page for /divisions route
 *
 * @returns {React.ReactElement}
 */
const DivisionsPage = () => {
  return <DivisionList />;
};

export default DivisionsPage;
