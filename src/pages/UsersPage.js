import UserList from '../components/Users/UserList';

/**
 * Container Page for /users routes
 *
 * @returns {React.ReactElement}
 */
const UsersPage = () => {
  return <UserList />;
};

export default UsersPage;
