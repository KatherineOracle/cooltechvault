import User from '../components/Users/User';

/**
 * Container Page for /users/user routes
 *
 * @returns {React.ReactElement}
 */
const UserPage = () => {
  return <User />;
};

export default UserPage;
