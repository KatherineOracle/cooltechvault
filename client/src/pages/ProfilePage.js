import UserProfile from '../components/Profile/ProfileForm';

/**
 * Container Page for /profile routes
 *
 * @returns {React.ReactElement}
 */
const ProfilePage = () => {
  return <UserProfile />;
};

export default ProfilePage;
