import Error404 from '../components/Errors/Error404';
/**
 * Container Page for unknown routes
 *
 * @returns {React.ReactElement}
 */
const PageNotFound = () => {
  return <Error404 />;
};

export default PageNotFound;
